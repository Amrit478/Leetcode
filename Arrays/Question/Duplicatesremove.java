
/*Input: nums = [1,1,2]
Output: 2, nums = [1,2]*/

public int Duplicatesremove(int [] nums) {
  /*I will use hashmap for remove the duplicates*/
  Hashmap<Integer> amrit = new Hashmap<>();
int i = 0;
  while(i < nums.length) {
    //if it duplicate than remove otherwise return the array using hashmap which should return the unique numbers only
    i++;
  }
}
